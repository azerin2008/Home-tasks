#include <iostream>//elementlerin cemi
using namespace std;

int main() {
    int s, n;
    int a [5] = {1, 2, 3, 4, 5};
    n = sizeof(a) / 4;
    for(int i = 0; i < n; i++){
         s = s + a[i];
    }
    cout << s;
    return 0;
}
