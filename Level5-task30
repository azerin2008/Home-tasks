#include <iostream>//3-e bolunen elementler
using namespace std;

int main() {
   int a[] = {1, 2, 3, 4, 6, 9};
   int n;
   n = sizeof(a) / sizeof(a[0]);
   for(int i = 0; i < n; i++){
       if(a[i] % 3 == 0){
           cout << a[i] << ", ";
       }
   }
    return 0;
}
