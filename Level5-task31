#include <iostream>//5-e bolunen elementler
using namespace std;

int main() {
   int a[] = {5, 10, 12, 15};
   int n;
   n = sizeof(a) / sizeof(a[0]);
   for(int i = 0; i < n; i++){
       if(a[i] % 5 == 0){
           cout << a[i] << ", ";
       }
   }
    return 0;
}
