#include <iostream>//maksimum element
using namespace std;

int main() {
    int s, b, c, n;
    int a [] = {3, 7, 1, 9, 5};
    n = sizeof(a) / 4;
    b = a[1];
    for(int i = 0; i < n; i++){
         if(b < a[i]){
             b = a[i];
         }
    }
    cout << "max: " << b;
    return 0;
}
